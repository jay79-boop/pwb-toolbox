-r requirements.txt
pytest
# Pinned: black's stable style changes with each January release, so an
# unpinned formatter would eventually fail CI on code nobody touched.
black==26.5.1

# tools/build_profit_planner.py writes an .xlsx workbook; the test suite
# builds it in memory rather than shipping a binary. Pinned for the same
# reason black is: CI installs fresh every run, so an unpinned dependency
# would eventually fail the job on code nobody touched.
openpyxl==3.1.5
