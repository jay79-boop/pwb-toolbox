# spec_desk/

The speculative paper-trading ledger lives here (`tools/spec_desk.py`).
Everything except this README is gitignored: trade records are personal,
and this fork is public. The ledger file is `spec_desk.json`.
