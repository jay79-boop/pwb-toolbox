---
status: active
project: meta
type: reference
---

# GitHub Links Catalog

Every GitHub link dropped in chat, in one place, so it doesn't have to be re-found by digging through session history. New links get added here as they come up — check this note before re-cataloging from scratch.

## Fully built & verified projects (own vault note)

These have a full project note with build/verify status — this table is just the index; go to the linked note for detail.

| Repo | Vault note |
|---|---|
| _(empty — fill in as projects get built and verified)_ | |

## Cloned or installed, no dedicated vault note yet

<!-- One bullet per repo: link, date, one-line description, what state it's in, where its status actually lives (this catalog / auto-memory / a run-skill). -->

## Reference / template sources

<!-- Repos dropped for inspiration/reference rather than to build — link + one line on why it's here. -->

## Maintenance

When a new GitHub link shows up in chat: add it here immediately (right section, or a new one if it doesn't fit). When a "not yet verified" entry gets built out into its own project note, move its row up to the top table and link out instead of duplicating detail. See [Clone-and-Run Calibration](..\06 - Working Style\Clone-and-Run Calibration.md) for how to decide *how much* verification a given link actually warrants.
