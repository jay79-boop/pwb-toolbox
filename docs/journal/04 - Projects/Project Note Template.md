---
status: active
project: <tag>
type: project
---

# <Project Name>

One-line description of what the tool/repo does ([owner/repo](https://github.com/owner/repo) — a short parenthetical on what it's for and, if relevant, what larger ecosystem/author it belongs to). Cloned to `<local path>`. Note which other project notes it's the same category as, if any (e.g. "same category as Other Project — explored via clone-and-run, not itself the main workstream").

**Status (<date>):** built and verified working / blocked / reference-only — state the real outcome, not the aspiration.

- What was actually run to verify it (install command, test suite, a real interaction — not just "read the README"). Cite pass/fail counts if there's a test suite.
- Any credential/provider blocker, named specifically (which env var, which service) — or "no credential blocker" if genuinely none.
- Real findings from actually running it (platform-specific failures, quirks, things the docs didn't mention) — this is the part worth writing down; it's what a doc read alone wouldn't have told you.

**Next step:** what's blocking further progress, or "none blocking" if fully explored for now.

<!-- Delete this comment block once filled in. Keep entries factual and dated — status changes over time, and a stale "verified working" note is worse than no note. -->
