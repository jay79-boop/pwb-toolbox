# trading_agents/

Output of `tools/trading_agents.py`: one JSON file per `analyze` run, named
`<ticker>_<date>.json`. Gitignored — these are personal proposals, not
reference data, and this fork is public.

Every file is a **signal**, never an instruction. Nothing in this repo reads
this directory automatically or acts on a file placed here.
