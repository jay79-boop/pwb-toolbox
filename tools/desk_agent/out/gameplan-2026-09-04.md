# Pre-market gameplan — 2026-09-04 (Friday), 07:02 CDT

**No gameplan. No levels in this file, nothing here is tradeable.** Step 1 of
`jobs/premarket.md` is still refused before it executes:

```
python tools/desk_levels.py levels NQ=F --markdown   -> requires approval
```

A headless run has nobody to answer a permission prompt, so that is a denial,
not a pause. The feed was never reached — this is not the job file's "feed
returned nothing" case.

The argument is made in full in `gameplan-2026-08-31.md`, `-09-01.md` and
`-09-02.md` and is **not repeated here**. A fifth essay to a reader that does not
exist changes nothing. Three new facts, then the fix, then nothing else.

## What is new

**1. The 09-02 one-paste fix was never applied.** Re-read `.claude/settings.json`
this morning: `permissions.allow` still ends its Bash grants at the two `runlog`
lines (5–6). No `desk_levels`, no `backtest_lab`. `settings.local.json` has grown
by twenty-odd PowerShell entries from interactive sessions since — so that file
*is* being written to, just never with this line.

**2. Yesterday's premarket did not fail on this blocker — it never started.**
The 2026-09-03 07:00 record is `claude exited non-zero: 1`, tail
*"You've hit your session limit · resets 7:10am"*. That is why there is no
`gameplan-2026-09-03.md`. Worth separating: the streak is four blocked weekday
mornings, but only three of them are the permission wall. **A scheduled
unattended job sharing a usage window with interactive work will lose that
race**, silently, and the only trace is one line in the run log.

**3. Every escalation route out of this job is now mapped and closed.** The
2026-09-02 journal run tried the last untried one — appending a guarded,
self-clearing block to `.claude/hooks/session-orient.sh`, the same channel the
night lab and the paper-book marker use, and the only one that provably reaches
the owner. **The permission system refused the write: `.claude/hooks` is
protected.** So: cannot run the tool, cannot edit the permission file, cannot
edit the launcher, cannot write the notification, and the Artifact ledger
`CLAUDE.md` names as the durable channel is unavailable headless. Five doors,
five closed. I retried none of them today and looked for a way around none of
them.

That is the finding worth a human's attention this morning. It is not about
`premarket`. **An unattended agent here has no working way to report that it is
broken to anyone who can fix it** — the run log and `out/` both file to the
weekly review, and the weekly review is not registered as a scheduled task
(`tools/register_desk_agent.ps1` lists Premarket, Alerts, Journal; no review).

## The fix, unchanged from 09-02, as one paste

```powershell
cd C:\Users\Gexio\OneDrive\pwb-toolbox; $f = '.claude\settings.json'; $t = [IO.File]::ReadAllText($f); if ($t.Contains('desk_levels.py')) { 'ALREADY THERE - nothing changed' } else { $a = '      "Bash(python3 -m tools.desk_agent.runlog:*)",'; $n = $a + "`r`n" + '      "Bash(python tools/desk_levels.py:*)",' + "`r`n" + '      "Bash(python3 tools/desk_levels.py:*)",' + "`r`n" + '      "Bash(python tools/backtest_lab.py:*)",' + "`r`n" + '      "Bash(python3 tools/backtest_lab.py:*)",'; $t2 = $t.Replace($a, $n); if ($t2 -eq $t) { 'FAILED - anchor line not found, nothing written' } else { [IO.File]::WriteAllText($f, $t2, (New-Object Text.UTF8Encoding $false)); 'ADDED - now ' + (Select-String -Path $f -Pattern 'desk_levels|backtest_lab').Count + ' matching lines' } }; python -c "import yfinance; print('yfinance OK', yfinance.__version__)"
```

Two lines of success: `ADDED - now 4 matching lines`, then `yfinance OK <ver>`.
A `ModuleNotFoundError` on the second line means the agent's `python` is the
system one, not the repo `.venv` — and then `run_job.ps1` also needs
`$RepoRoot\.venv\Scripts` prepended to `PATH` for the agent invocation
(lines 98–99 resolve that interpreter already but use it only for the fallback
failure record). Still not made here: it changes which interpreter every future
scheduled run uses, cannot be tested from inside the run it would affect, and
breaks all jobs rather than one if wrong.

## Deliberately not done

- **No TradingView, no `tv_launch`, no chart, no screenshot** — correct per the
  job file and per a task stating there may be no desktop. Not an available
  fallback that was skipped.
- **No substitute feed.** Alpha Vantage is connected. Hand-rolled session levels
  from a second vendor, unattended and without any of the coverage
  `tests/test_desk_levels.py` gives the real tool, look exactly like tested
  numbers and are not.
- **No route around the denial** — not a different interpreter spelling, not
  `python -m`, not another tool used to fetch the same bars. Going around a
  permission denial with a tool that happens to be allowed is the same
  violation wearing a hat.
- **No retry of the four closed doors.** Re-proving a closed door burns a run.
